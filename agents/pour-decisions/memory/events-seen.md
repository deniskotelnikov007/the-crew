# Events seen

Every event that has appeared in a digest. `id` = `<source>:<date>:<slug>` (see scan-tastings). One row per event; add rows, don't rewrite old ones.

| id | first seen | title | score |
|---|---|---|---|
